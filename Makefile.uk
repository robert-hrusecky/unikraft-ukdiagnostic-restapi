$(eval $(call addlib_s,libUKDIAGREST,$(CONFIG_LIBUKDIAGREST)))

# LIBUKDIAGREST_CFLAGS-$(call gcc_version_ge,8,0) += -Wno-cast-function-type

CINCLUDES-$(CONFIG_LIBUKDIAGREST) += -I$(LIBUKDIAGREST_BASE)/include
CXXINCLUDES-$(CONFIG_LIBUKDIAGREST) += -I$(LIBUKDIAGREST_BASE)/include
LIBUKDIAGREST_SRCS-y += $(LIBUKDIAGREST_BASE)/rest_server.c
LIBUKDIAGREST_SRCS-y += $(LIBUKDIAGREST_BASE)/json_parser.c
